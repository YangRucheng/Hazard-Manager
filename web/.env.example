# 前端构建环境变量示例（复制为 web/.env.production 或在构建命令前直接传入 HOST）
# HOST 为后端接口地址；不配置时前端走相对 /api/v1（nginx 反代）
# 用法：HOST=https://hazard-manager.qcloud.19890605.xyz pnpm build
HOST=https://hazard-manager.qcloud.19890605.xyz